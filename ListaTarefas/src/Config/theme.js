import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    michas: {
      main: "#FF0000",
      contrastText: "#ffffff",
    },
    roxo: {
      main: "rgb(41, 41, 80)",
      contrastText: "#ffffff",
    },
    rosa: {
      main: "#7B2CBF",
      contrastText: "#ffffff",
    },
  },
});
